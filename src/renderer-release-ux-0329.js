'use strict';
(function installReleaseUx0329(){
  if(window.__ec0329ReleaseUxInstalled)return;
  if(!window.ECAPI||typeof settings==='undefined'||!settings||!window.__ec0329Installed||typeof rssCoreSignature!=='function'){setTimeout(installReleaseUx0329,120);return;}
  window.__ec0329ReleaseUxInstalled=true;
  const q=s=>document.querySelector(s),qa=s=>[...document.querySelectorAll(s)];
  const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  let rssBaseline=rssCoreSignature(settings?.rssFeeds||[]);settings.__savedRssSignature=rssBaseline;

  function profileColor(el){return el?.style?.color||getComputedStyle(el||document.documentElement).color||'#aaa';}
  function colorContrast(hex,bg='#181818'){const rgb=v=>{const m=String(v||'').match(/^#([0-9a-f]{6})$/i);if(!m)return null;const n=parseInt(m[1],16),c=[(n>>16)&255,(n>>8)&255,n&255].map(x=>{x/=255;return x<=.04045?x/12.92:Math.pow((x+.055)/1.055,2.4);});return .2126*c[0]+.7152*c[1]+.0722*c[2];},a=rgb(hex),b=rgb(bg);if(a==null||b==null)return 0;return(Math.max(a,b)+.05)/(Math.min(a,b)+.05);}
  function profileColorReadable(hex){return colorContrast(hex)>=3;}
  function polishDock(){const btn=q('#ec29ProfileButton'),name=q('#ec29ProfileName');if(!btn||!name)return;let dot=btn.querySelector('.ec29-current-dot');if(!dot){dot=document.createElement('span');dot.className='ec29-current-dot';btn.insertBefore(dot,name);}const color=profileColor(name);dot.style.background=color;btn.style.setProperty('--profile-color',color);const arrow=btn.querySelector('.ec29-profile-arrow');if(arrow&&arrow.textContent!=='⌃')arrow.textContent='⌃';}
  function polishPopover(pop){if(!pop||pop.dataset.polished==='1')return;pop.dataset.polished='1';pop.setAttribute('role','menu');const add=pop.querySelector('[data-action="add"]');if(add){add.classList.add('ec29-add-profile-row');const disabled=add.disabled,label=add.textContent.trim();add.innerHTML=`<span class="ec29-menu-icon">＋</span><span class="ec29-menu-copy"><b>${disabled?'Límite de perfiles alcanzado':'+ Añadir nuevo perfil'}</b><small>${disabled?esc(label):'Crea un espacio independiente'}</small></span>`;}const profileButtons=[...pop.querySelectorAll('[data-profile]')];profileButtons.forEach(btn=>{const oldName=btn.querySelector('span:first-child'),oldCheck=btn.querySelector('.ec29-check'),name=oldName?.textContent?.trim()||'Perfil',color=profileColor(oldName),active=String(oldCheck?.textContent||'').includes('✓');btn.classList.add('ec29-profile-option');btn.classList.toggle('active',active);btn.style.setProperty('--profile-color',color);btn.innerHTML=`<span class="ec29-option-dot" style="background:${color}"></span><span class="ec29-option-copy"><b style="color:${color}">${esc(name)}</b><small>${active?'Perfil activo':'Perfil disponible'}</small></span>${active?'<span class="ec29-active-pill">ACTIVO</span>':'<span class="ec29-option-chevron">›</span>'}`;});const manage=pop.querySelector('[data-action="manage"]');if(manage){const counter=document.createElement('div');counter.className='ec29-profile-count';counter.textContent=`${profileButtons.length} perfil${profileButtons.length===1?'':'es'}`;manage.parentNode.insertBefore(counter,manage);manage.innerHTML='<span class="ec29-menu-icon">⚙</span><span class="ec29-menu-copy"><b>Administrar perfiles</b><small>Editar, duplicar, exportar o eliminar</small></span><span class="ec29-option-chevron">›</span>';}}
  function polishAdmin(modal){if(!modal||modal.dataset.adminPolished==='1'||!modal.querySelector('.ec29-admin-list'))return;modal.dataset.adminPolished='1';const heading=modal.querySelector('h2');if(heading){const match=heading.textContent.match(/(\d+)\/(\d+)/);heading.textContent='Administrar perfiles';if(match){const meta=document.createElement('div');meta.className='ec29-admin-summary';meta.textContent=`${match[1]} de ${match[2]} perfiles utilizados`;heading.insertAdjacentElement('afterend',meta);}}qa('.ec29-admin-row').filter(row=>modal.contains(row)).forEach(row=>{const main=row.querySelector('.ec29-admin-main'),title=row.querySelector('.ec29-admin-title'),meta=row.querySelector('.ec29-admin-meta');if(!main||!title||main.dataset.polished==='1')return;main.dataset.polished='1';const active=/ACTIVO/i.test(meta?.textContent||''),color=profileColor(title);row.classList.toggle('active',active);row.style.setProperty('--profile-color',color);const dot=document.createElement('span');dot.className='ec29-admin-dot';dot.style.background=color;main.insertBefore(dot,main.firstChild);const copy=document.createElement('div');copy.className='ec29-admin-copy';while(dot.nextSibling)copy.appendChild(dot.nextSibling);main.appendChild(copy);if(meta){meta.textContent=active?'Perfil activo':'Perfil disponible';meta.classList.toggle('active',active);}if(active){const pill=document.createElement('span');pill.className='ec29-admin-active-pill';pill.textContent='ACTIVO';main.appendChild(pill);}});}
  function polishProfileForm(modal){if(!modal||modal.dataset.colorGuard==='1'||!modal.querySelector('#ec29CustomColor')||!modal.querySelector('#ec29Save'))return;modal.dataset.colorGuard='1';const custom=modal.querySelector('#ec29CustomColor'),save=modal.querySelector('#ec29Save'),palette=modal.querySelector('#ec29Palette');const warning=document.createElement('div');warning.className='ec29-color-warning';warning.textContent='Elige un color más claro para que el perfil sea legible sobre la interfaz oscura.';palette?.insertAdjacentElement('afterend',warning);const validate=()=>{const color=String(custom.value||'#000000').toUpperCase(),readable=profileColorReadable(color);warning.classList.toggle('visible',!readable);save.disabled=!readable;save.title=readable?'':'El color seleccionado tiene poco contraste.';return readable;};custom.addEventListener('input',()=>requestAnimationFrame(validate));palette?.addEventListener('click',()=>setTimeout(validate,0));const original=save.onclick;save.onclick=function(e){if(!validate()){e?.preventDefault?.();return;}return original?.call(this,e);};validate();}

  function rssDirty(){try{return rssCoreSignature(settings?.rssFeeds||[])!==rssBaseline;}catch{return false;}}
  function ensureDirtyBadge(){const bar=q('#tab-settings .settings-save');if(!bar)return null;let badge=q('#ec29UnsavedBadge');if(!badge){badge=document.createElement('span');badge.id='ec29UnsavedBadge';badge.className='ec29-unsaved-badge';badge.textContent='Cambios sin guardar';bar.insertBefore(badge,bar.firstChild);}return badge;}
  function refreshDirtyUi(){const dirty=rssDirty(),badge=ensureDirtyBadge(),save=q('#save');if(badge)badge.classList.toggle('visible',dirty);if(save)save.classList.toggle('ec29-save-dirty',dirty);return dirty;}
  function installDirtyTracking(){ensureDirtyBadge();document.addEventListener('input',e=>{if(e.target?.closest?.('#feeds'))requestAnimationFrame(refreshDirtyUi);},true);document.addEventListener('change',e=>{if(e.target?.closest?.('#feeds'))requestAnimationFrame(refreshDirtyUi);},true);if(typeof saveSettings==='function'&&!saveSettings.__ec29DirtyWrapped){const base=saveSettings,wrapped=async function(...args){const r=await base.apply(this,args);rssBaseline=rssCoreSignature(settings?.rssFeeds||[]);settings.__savedRssSignature=rssBaseline;refreshDirtyUi();return r;};wrapped.__ec29DirtyWrapped=true;saveSettings=wrapped;}const start=q('#processStart');if(start&&!start.dataset.ec29Guard){start.dataset.ec29Guard='1';const base=start.onclick;start.onclick=async function(e){if(refreshDirtyUi()){e?.preventDefault?.();const save=q('#save');save?.classList.add('ec29-attention');setTimeout(()=>save?.classList.remove('ec29-attention'),1800);if(typeof status==='function')status('Hay cambios en las fuentes sin guardar. Guarda los cambios antes de iniciar la preparación.');const detail=q('#processingDetail');if(detail)detail.textContent='Fuentes modificadas sin guardar · guarda los cambios para usarlas en este perfil.';return;}const feeds=(settings?.rssFeeds||[]).filter(f=>f?.enabled!==false&&String(f?.url||'').trim());if(!feeds.length){e?.preventDefault?.();if(typeof status==='function')status('Este perfil no tiene fuentes de noticias configuradas.');const detail=q('#processingDetail');if(detail)detail.textContent='Este perfil no tiene fuentes configuradas. Añade una fuente en Ajustes y guarda los cambios.';return;}return base?.call(this,e);};}refreshDirtyUi();}

  let localMissing=false,optimizerApplying=false;const missingOptimizerText='IA local no instalada todavía. Descarga y verifica Qwen para habilitar la optimización automática.';function setTextIfChanged(el,text){if(el&&el.textContent!==text)el.textContent=text;}
  async function normalizeLocalOptimizer(){let st;try{st=await window.ECAPI?.localStatus?.();}catch{return;}const wasMissing=localMissing;localMissing=!st?.model;const badge=q('#ecOptimizeState0321'),box=q('#ecOptimizeResult0321'),btn=q('#optimizeEc0321');optimizerApplying=true;try{if(localMissing){if(badge){setTextIfChanged(badge,'IA LOCAL NO INSTALADA');if(badge.className!=='mini-pill neutral')badge.className='mini-pill neutral';}if(box&&!box.dataset.live)setTextIfChanged(box,missingOptimizerText);if(btn){btn.disabled=true;btn.title='Instala primero la IA local para habilitar la optimización.';}return;}if(wasMissing){if(badge){setTextIfChanged(badge,'SIN OPTIMIZAR');if(badge.className!=='mini-pill')badge.className='mini-pill';}if(box&&!box.dataset.live)setTextIfChanged(box,'IA local lista. Ya puedes optimizar GEC para esta computadora.');}if(btn){btn.disabled=false;btn.title='';}}finally{optimizerApplying=false;}}
  function installOptimizerObserver(){normalizeLocalOptimizer();const box=q('#ecOptimizeResult0321');if(box){new MutationObserver(()=>{if(localMissing&&!optimizerApplying&&box.textContent!==missingOptimizerText)setTimeout(normalizeLocalOptimizer,0);}).observe(box,{childList:true,subtree:true,characterData:true});}window.ECAPI?.on?.('local:event',e=>{const t=String(e?.type||'');if(/download|model|ready/i.test(t))setTimeout(normalizeLocalOptimizer,250);});}

  function inspect(node=document){polishDock();const pop=node.matches?.('.ec29-popover')?node:node.querySelector?.('.ec29-popover');if(pop)polishPopover(pop);const modal=node.matches?.('.ec29-modal')?node:node.querySelector?.('.ec29-modal');if(modal){polishAdmin(modal);polishProfileForm(modal);}}
  const observer=new MutationObserver(records=>{for(const r of records)for(const n of r.addedNodes)if(n.nodeType===1)inspect(n);polishDock();});observer.observe(document.documentElement,{childList:true,subtree:true});const name=q('#ec29ProfileName');if(name)new MutationObserver(polishDock).observe(name,{attributes:true,childList:true,characterData:true,subtree:true});inspect();installDirtyTracking();setTimeout(installOptimizerObserver,450);
})();
