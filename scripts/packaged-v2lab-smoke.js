'use strict';
const fs=require('fs'),path=require('path'),os=require('os');
const {app}=require('electron');
const resourcesDir=path.resolve(process.argv[2]||path.join('dist','win-unpacked','resources')),appRoot=path.join(resourcesDir,'app.asar'),assert=(v,m)=>{if(!v)throw new Error(m);};
app.whenReady().then(()=>{let tmp='';try{
  const required=['src/bootstrap-v2lab.js','src/services/releaseV2Lab.js','src/services/ttsLabRuntime.js','src/renderer-v2lab.js','src/control-v2lab.css','src/tts_lab_worker.py','scripts/check-v2lab.js'];for(const rel of required)assert(fs.existsSync(path.join(appRoot,rel)),`Falta en app.asar: ${rel}`);
  const pkg=JSON.parse(fs.readFileSync(path.join(appRoot,'package.json'),'utf8'));assert(/^2\.0\.0-lab\./.test(pkg.version),'Versión empaquetada no es V2 TTS Lab');assert(pkg.main==='src/bootstrap-v2lab.js','Bootstrap V2 Lab no es entry point');assert(/V2\.0 TTS Lab/i.test(pkg.build?.productName||''),'Nombre de producto no distingue TTS Lab');
  const worker=path.join(resourcesDir,'runtime','tts-lab','tts_lab_worker.py');assert(fs.existsSync(worker),'Worker de TTS Lab no está incluido en extraResources');assert(fs.statSync(worker).size>1000,'Worker de TTS Lab parece incompleto');
  tmp=fs.mkdtempSync(path.join(os.tmpdir(),'gec-v2tts-pack-'));const {TTSLabRuntime,ENGINES}=require(path.join(appRoot,'src','services','ttsLabRuntime.js'));const rt=new TTSLabRuntime({resourcesDir,dataDir:tmp});const st=rt.status('kokoro');assert(st.python,'TTS Lab no encuentra Python portable');assert(st.workerScript,'TTS Lab no encuentra su worker empaquetado');assert(ENGINES.chatterbox&&ENGINES.qwen3tts,'Catálogo experimental incompleto');assert(st.engines.some(x=>x.id==='chatterbox'&&!x.installed)&&st.engines.some(x=>x.id==='qwen3tts'&&!x.installed),'Los motores experimentales deben empezar como instalación bajo demanda');
  const {normalizeProfileTts}=require(path.join(appRoot,'src','services','releaseV2Lab.js'));const profile=normalizeProfileTts({engine:'qwen3tts',style:'news',referenceVoiceId:'ref-test'});assert(profile.engine==='qwen3tts'&&profile.referenceVoiceId==='ref-test','Normalización de perfil TTS Lab falló');
  fs.rmSync(tmp,{recursive:true,force:true});console.log('PACKAGED V2 TTS LAB OK · herencia 0.3.31 · runtime separado · motores bajo demanda · perfil multi-TTS');app.exit(0);
}catch(e){console.error(e.stack||e);try{if(tmp)fs.rmSync(tmp,{recursive:true,force:true});}catch{}app.exit(1);}});
