name: Build Windows Portable

on:
  workflow_dispatch:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  build:
    runs-on: windows-latest
    timeout-minutes: 60
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v7
        with:
          node-version: '22.x'

      - name: Setup Python for embedded Kokoro runtime
        uses: actions/setup-python@v6
        with:
          python-version: '3.12'

      - name: Install Node dependencies
        run: npm install

      - name: Audit production dependencies
        run: npm audit --omit=dev --audit-level=high

      - name: Prepare bundled local runtimes
        shell: powershell
        run: ./scripts/prepare-windows-runtime.ps1

      - name: Static and regression checks
        run: npm run check

      - name: Build portable EXE
        env:
          CSC_IDENTITY_AUTO_DISCOVERY: 'false'
        run: npm run dist:portable

      - name: Windows packaged UI, Output, normalizer, Kokoro, pronunciation, ONNX and NVIDIA fallback smoke tests
        shell: powershell
        timeout-minutes: 15
        run: |
          $resources = Join-Path $PWD 'dist\win-unpacked\resources'
          $exe = Join-Path $PWD 'dist\win-unpacked\EC Automatic News.exe'
          if (-not (Test-Path $exe)) { throw "Missing executable: $exe" }
          if (-not (Test-Path (Join-Path $resources 'app.asar'))) { throw 'Missing packaged app.asar' }
          npx electron scripts/packaged-smoke.js $resources
          if ($LASTEXITCODE -ne 0) { throw "Packaged smoke test failed with code $LASTEXITCODE" }
          npx electron scripts/packaged-0324-smoke.js $resources
          if ($LASTEXITCODE -ne 0) { throw "Packaged 0.3.24 smoke test failed with code $LASTEXITCODE" }
          npx electron scripts/packaged-0328-smoke.js $resources
          if ($LASTEXITCODE -ne 0) { throw "Packaged 0.3.28 smoke test failed with code $LASTEXITCODE" }
          npx electron scripts/packaged-compat-0328.js $resources
          if ($LASTEXITCODE -ne 0) { throw "Packaged 0.3.25-0.3.27 compatibility test failed with code $LASTEXITCODE" }
          npx electron scripts/packaged-pronunciation-smoke.js $resources
          if ($LASTEXITCODE -ne 0) { throw "Packaged pronunciation smoke test failed with code $LASTEXITCODE" }
          npx electron scripts/packaged-onnx-smoke.js $resources
          if ($LASTEXITCODE -ne 0) { throw "Packaged ONNX smoke test failed with code $LASTEXITCODE" }
          npx electron scripts/packaged-gpu-smoke.js $resources
          if ($LASTEXITCODE -ne 0) { throw "Packaged NVIDIA fallback smoke test failed with code $LASTEXITCODE" }

      - name: Upload portable folder
        uses: actions/upload-artifact@v7
        with:
          name: EC-Automatic-News-Portable-Folder
          path: dist/win-unpacked/**
          if-no-files-found: error
          retention-days: 14

      - name: Upload portable EXE
        uses: actions/upload-artifact@v7
        with:
          name: EC-Automatic-News-Windows-Portable-EXE
          path: dist/*.exe
          if-no-files-found: error
          retention-days: 14

# CI trigger after Windows PowerShell encoding fix
